!!!
%html
  %head
    %title GRUNIV
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags
  %body
    %header.navbar.navbar-default{:role => "navigation"}
      .navbar-inner
        .container
          #logo.navbar-brand= link_to "GRUNIV", root_path
          %nav.collapse.navbar-collapse.navbar-ex1-collapse
            - if user_signed_in?
              %ul.nav.navbar-nav.navbar-right
                %li
                  = link_to new_link_path do
                    %i.glyphicon.glyphicon-magnet Enlace
                %li
                  = link_to new_pin_path do
                    %span.glyphicon.glyphicon-list-alt
                    Clasificados
                %li
                  = link_to edit_user_registration_path do
                    %i.glyphicon.glyphicon-user Cuenta
                %li
                  = link_to destroy_user_session_path, :method => :delete do
                    %i.glyphicon.glyphicon-ban-circle Desconectarse
            - else
              %ul.nav.navbar-nav.pull-right
                %li
                  = link_to new_user_registration_path do
                    %i.glyphicon.glyphicon-pencil  Registrarse
                %li
                  = link_to new_user_session_path do
                    %i.glyphicon.glyphicon-globe  Conectarse
      = yield :landing
    - if user_signed_in?
      :javascript
        window.scrollback = {"room":"gruniv-chat","form":"canvas","minimize":true};(function(d,s,h,e){e=d.createElement(s);e.async=1;e.src=(location.protocol === "https:" ? "https:" : "http:") + "//scrollback.io/client.min.js";d.getElementsByTagName(s)[0].parentNode.appendChild(e);}(document,"script"));
    #main_content.container
      - flash.each do |name, msg|
        = content_tag(:div, msg, class: "alert alert-info")
      #content.col-md-9.center-block
        = yield